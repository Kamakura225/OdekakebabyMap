<div class="container my-5">
  <h1 class="mb-4 text-center"><%= @place.name %> の削除リクエスト</h1>

  <%= form_with model: @place_delete_request, url: public_place_place_delete_requests_path(@place), local: true do |f| %>
    <div class="card shadow-sm">
      <div class="card-body">
        <p>この施設の削除をリクエストする理由を記入してください。</p>

        <div class="form-group mb-4">
          <%= f.label :reason, "理由", class: "form-label fw-bold" %>
          <%= f.text_area :reason, class: "form-control", rows: 5, placeholder: "例：施設が閉鎖されている、情報が虚偽である など", required: true %>
        </div>

        <div class="d-flex justify-content-center">
          <%= f.submit "削除リクエストを送信", class: "btn btn-danger btn-lg w-50" %>
        </div>
      </div>
    </div>
  <% end %>

  <div class="d-flex justify-content-center mt-4">
    <%= link_to "戻る", public_place_path(@place), class: "btn btn-secondary w-50" %>
  </div>
</div>