<%= form_with(model: @place, url: update_status_admin_place_path(@place), method: :patch) do |f| %>
  <%= f.label :status, "ステータス" %>
  <%= f.select :status, Place.statuses.keys.map { |status| [status.humanize, status] }, { selected: @place.status } %>
  <%= f.submit "ステータス更新" %>
<% end %>